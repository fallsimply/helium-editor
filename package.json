{
	"name": "helium-editor-workspace",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"demo:dev": "npm run dev --workspace=demo",
		"demo:build": "npm run build --workspace=demo",
		"core:build": "npm run build --workspace=@fallsimply/helium"
	},
	"workspaces": [
		"packages/*"
	]
}
